#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -ueo pipefail

BUILD_DIR=$1

"$BUILD_DIR/.heroku/bin/heroku" plugins
"$BUILD_DIR/.heroku/bin/heroku" plugins:install heroku-pg-extras
"$BUILD_DIR/.heroku/bin/heroku" plugins
